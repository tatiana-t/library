import React from "react";
import Chip  from "components/ui/chip";

export const chip = () => <Chip text='перечитать' isActive={false} onClick={()=>{}} isShowRemove={false}/>;
// export const checkboxError = () => <Chip text='stories-checkbox-error'/>;

export default {
    title: "Chip",
};